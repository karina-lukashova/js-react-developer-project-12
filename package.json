{
  "name": "@hexlet/code",
  "version": "1.0.0",
  "description": "Hexlet slack project",
  "main": "index.js",
  "scripts": {
    "build": "npm run build --prefix slack-clone",
    "start": "npx start-server -s ./slack-clone/dist",
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "cd slack-clone && npm ci"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@hexlet/chat-server": "^2.0.4"
  }
}
